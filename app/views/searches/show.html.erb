<h2>Search Results</h2>
  <% if @found_results.empty? %>
    <h3 class="label">No results found.</h3>
  <% else %>
    <ul>
    <% @found_results.each do |found_result| %>
    <li><%= link_to  found_result.title %></li>
    <% end %>
    </ul>
  <% end %>
