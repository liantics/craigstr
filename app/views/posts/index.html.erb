<h2><%= link_to "Locations", :locations %></h2>
<h2> Posts </h2>

All your posts are belong to us

<h2>Add a Post</h2>

<%= form_for @post do |form| %>
  <%= render "error_messages", target: @post %>
  <div>
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>
  <div>
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>
  <div>
    <%= form.submit  %>
  </div>
<% end %>

<h2>All Posts</h2>
<ul>
  <% @posts.each do |post| %>
    <li><%= post.title %></li>
  <%end%>
</ul>
